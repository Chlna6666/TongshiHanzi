# Contributing

Use Java and XML for the Android application. Keep database and file I/O off the main thread. Add tests for search changes. Do not add dictionary data without documented provenance and a compatible license. Run `gradle clean test assembleDebug` before submitting changes.
